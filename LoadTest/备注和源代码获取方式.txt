  因为自己需要写一个Http服务器，不知道能接受多少并发和性能，于是自己写了一个简单的Http Post/Get并发访问测试软件。
项目是基于.Net Framework4 C#开发的，所以运行设备需事先安装该环境，也就40M，自己百度一下。

  关于请求数，多说一句，是依次执行N个异步处理的HTTP请求，速度接近于并发，但不是真正的并发，如果测试服务器和本机是同一台设备，也肯定是会受到这个软件影响的。

  如果那位需要源代码的话，可以免费提供，为了避免麻烦，这里只上传EXE文件了。

  联系地址：http://blog.csdn.net/song_qingwei